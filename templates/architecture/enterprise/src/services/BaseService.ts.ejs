<% if (architecture === 'class') { %>
export class BaseService {
  // Common service methods can be implemented here
  // e.g. standardized external API caller, logging integrations, etc.
}
<% } else { %>
// Common service methods can be implemented here
// e.g. standardized external API caller, logging integrations, etc.
<% } %>
