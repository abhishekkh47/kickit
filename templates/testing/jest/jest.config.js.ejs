/** @type {import('jest').Config} */
module.exports = {
<% if (language === 'typescript') { %>
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/tests'],
  testMatch: ['**/*.test.ts'],
<% } else { %>
  testEnvironment: 'node',
  roots: ['<rootDir>/tests'],
  testMatch: ['**/*.test.js'],
<% } %>
};
